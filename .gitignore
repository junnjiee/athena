# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (never commit secrets like DATABASE_URL)
.env
.env.local
!.env.example

# Terrain exports fed to the engine. Multi-megabyte generated data, and each
# one describes a single AO, so they belong beside a run rather than in history.
payload.txt
*.payload.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
